import sys
import os


INTERP = os.path.join(os.environ['HOME'], 'www/bunnyapprovesthesechanges.com', 'bin', 'python')
if sys.executable != INTERP:
    os.execl(INTERP, INTERP, *sys.argv)
#sys.path.append(os.getcwd())
from bunny import app as application
